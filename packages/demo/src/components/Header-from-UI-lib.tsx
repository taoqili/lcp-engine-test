import React from "react";
import { Header } from "@lcp/libs";

export const HeaderFromUILib = () => {
  return (
    <Header
      onLogin={() => {}}
      onCreateAccount={() => {}}
      onLogout={() => {}}
    >
    </Header>
  );
};
